# Scratch space — research, drafts, session notes. Never published.
.local/
CLAUDE.local.md

# fact-sheet and paper-trail write here. This repo's own output isn't worth
# publishing — a stack sheet for "POSIX sh and markdown" is a punchline, and a
# decision log for five files is worse. In a real project, commit these.
docs/
